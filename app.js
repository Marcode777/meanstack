angular.module("meanstack", []) //the array brackets are needed, this is a requirement because it indicates what dependencies may be required, if there are no dependenies, the brackets must still be included but can be left empty
  .controller("MeanListController", function(){

  });